import styled from "@emotion/styled";

export const DownloadInput = styled("input")({
  borderRadius: "10px",
  fontWeight: "600",
  maxWidth: "fit-content",
  minWidth: "160px",
  minHeight: "40px",
  border: "none",
  outline: "none",
  cursor: "pointer",
  clipPath: "inset(50%)",
  position: "absolute",
});
